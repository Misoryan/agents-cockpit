# 口令 / 本机配置(切勿上传)
auth.txt
.auth
*.key

# 本机配置(从 config.example 复制而来,勿上传)
config.ini


# 二进制(需自行下载,不入库)
ttyd
ttyd.exe
codex
codex.exe

# Python
__pycache__/
*.pyc

# 运行时临时文件
.sid
*.log
.tmp*
.ttyd.html
.term.html

# 运行时会话状态(注册表 + scrollback,软重启重连用)
.agent-cockpit/

# 操作系统
.DS_Store
Thumbs.db
