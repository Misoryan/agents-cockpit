# 登录凭证文件。复制本文件为 auth.txt,改成你自己的账号。
# auth.txt 已在 .gitignore 中,不会被上传。
# 建议用强口令:登录后可在你的电脑上执行任意命令。
#
# 每行一个用户,格式「用户名:凭证」,行首 # 是注释;可写多行 = 多用户。
# 凭证支持两种写法:
#
#   1) 明文(兼容旧版,最简单):
#        codex:your-strong-password
#
#   2) PBKDF2 哈希(推荐:即便 auth.txt 泄露,也拿不到原口令):
#        codex:$pbkdf2$120000$<salt_b64>$<hash_b64>
#      用下面命令生成(把「你的密码」换成真实口令),会输出一长串 $pbkdf2$... :
#        python -c "import common; print(common.hash_password('你的密码'))"
#      把它整行粘到用户名后面,即 codex:$pbkdf2$120000$xxxx$yyyy
codex:change-me-to-a-strong-password
